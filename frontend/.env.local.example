# Gốc API của backend. Phải khớp API_PUBLIC_URL trong backend/.env.
# Biến có tiền tố NEXT_PUBLIC_ được nhúng vào bundle trình duyệt lúc build,
# nên TUYỆT ĐỐI không đặt khoá bí mật ở đây.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
